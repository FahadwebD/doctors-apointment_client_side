import React from 'react';

const Appointment = () => {
    const [date, setDate] = React.useState(new Date());
    
    return (
        <div>
            <h1>Appointments</h1>
        </div>
    );
};

export default Appointment;